name: Docker Image CI

on:
  push:
    branches: [ "ci-test" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag toolbox:$(date +%s)
